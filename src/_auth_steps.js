/*======================= Auth Steps =========================== 
    * 1. Create a new firebase project in console.firebase.google.com;
    * 2. npm install firebase;
    * 3. Create firebase.init.js and import getAuth to export auth;
    * 4. firebase project > Authentication > enable Email and password auth;
    * 5. Create & design Login, Signup component, setup route;
    * 6. attach form field handler and form submit handler;
    * 7. install react firebase hooks : npm install --save react-firebase-hooks;
    * 8. useCreateUserWithEmailAndPassword from react-firebase-hooks for user sign up;
    * 9. if user is created redirect to the expected or setted page;
    * 10. useSignInWithEmailAndPassword from react-firebase-hooks for user sign in;
    * 11. Create RequireAuth component > check user exists also track user location;
    * 12. In route wrap protected component by using RequireAuth Component;
    *
================================================================*/

/*======================= Firebase Hosting steps ========================
    * 1. npm install -g firebase-tools
    * 2. firebase login;
    * 3. firebase init;
    * 4. firebase deploy;
    * 
 ================================================================*/